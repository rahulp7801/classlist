// next.config.js

const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'],
    },
    css: [
      // Path to your global CSS file (optional, defaults to 'global.css')
      './global.css',
    ],
  };
  
  export default nextConfig;